--
alwaysApply: true
---
# 🛰️ FindIt 项目全局核心记忆卡 (Project Memory)

## 1. 基础背景与主权人
- **项目名称**：FindIt 视觉寻物小助手
- **核心定位**：全家多端设备共享、图片永不丢失的智能视觉寻物保险箱。
- **当前状态**：云端完全体已上线（ImgBB 图床 + GitHub 云账本 + GLM 视觉识别）。

## 2. 核心技术栈与死锁规则 (Tech Stack Lock)
AI 必须严格遵守以下技术选型，严禁引入任何未授权的重型第三方库：
- **Web 框架**：纯 Python 的 `Streamlit`（完全依赖原生组件，禁止编写复杂的自定义 HTML/Flexbox 布局）。
- **图片永久存储**：`ImgBB` 官方 API（通过 base64 编码图片流进行 POST 上传）。
- **AI 视觉识别**：Google Gemini `gemini-3.5-flash`（云端服务器调用，手机无需 VPN）。
- **部署方式**：Streamlit Community Cloud（`share.streamlit.io`），手机通过 HTTPS 公网 URL 访问。
- **密码箱调用**：必须通过 `st.secrets` 安全获取密钥，严禁把密码硬编码在代码中。
  - `st.secrets["IMGBB_API_KEY"]` -> ImgBB 密钥
  - `st.secrets["GITHUB_TOKEN"]` -> GitHub 密令
  - `st.secrets["GITHUB_REPO"]` -> 远程仓库名（格式：用户名/仓库名）
  - `st.secrets["GEMINI_API_KEY"]` -> Google AI Studio Gemini 密钥
- **中心数据库**：GitHub 仓库根目录下的 `findit_db.json` 公用数据中心账本。

## 3. 数据契约与账本格式 (Data Schema)
云账本 `findit_db.json` 中的每条物品数据必须严格保持以下字典格式：
```json
{
  "id": "UUID 或 时间戳字符串",
  "name": "物品名称(支持模糊搜索)",
  "location": "放置位置(支持模糊搜索)",
  "img_url": "由 ImgBB 返回的永久 https://i.ibb.co/ 开头的图片直链",
  "created_at": "YYYY-MM-DD HH:MM:SS"
}