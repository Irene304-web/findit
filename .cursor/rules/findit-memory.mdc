---
description: 寻物小助手(FindIt)智能寻物双重过滤与图片修复业务规则
globs: *.py
alwaysApply: true
---
# 🛰️ FindIt 智能寻物多维过滤与视觉修复记忆卡

## 1. 基础配置与清爽样式纪律
- **网页副标题**：锁死为 `"家庭云端收纳，AI智能识物存储，轻松记得东西放在哪里"`。
- **寻物检索按钮**：智能寻物页面的刷新检索按钮文案必须严格修改为 `"🔍 一键找到它"`。
- **界面样式**：保持原汁原味的原生轻量文字布局，严禁引入任何固定底部的重型 CSS 样式。

## 2. 智能寻物“双重过滤”与“精准显图”重构规范
1. **多家庭检索联动**：在 `render_search_tab()` 智能寻物页面顶部，必须在搜索框上方或并排引入一个家庭组筛选下拉框（`st.selectbox`）。其选项为 `["全部家庭组"] + st.session_state.all_homes`。
2. **过滤过滤机制**：物品数据池（`results`）的清洗必须同时支持关键词和家庭组。若选中了特定家庭空间，则只保留 `item.get("home", "默认家庭")` 与选中空间完全一致的条目；若为 `"全部家庭组"`，则全局模糊检索。
3. **精准显图控制（Bug 修复）**：在遍历 `results` 的循环内部，**必须且仅在用户输入了有效搜索关键词（即 `keyword.strip()` 不为空）且 `img_url` 存在时**，才允许渲染 `st.image(img_url, use_container_width=True)` 展示图片！若关键词为空，循环内部严禁渲染图片，只展示包含物品名称、家庭标签、放置位置和创建时间的纯文字卡片。
4. **单项无痕删除**：每个物品卡片下方保留红色的“❌ 删除该物品”按钮，点击后依据 id 抹除，立即 commit 同步至 GitHub 并执行 `st.rerun()`。